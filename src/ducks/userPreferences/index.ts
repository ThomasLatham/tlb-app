/* eslint-disable import/no-anonymous-default-export */
import { actions, namespace, reducer } from "./userPreferences.slice";
import selectors from "./userPreferences.selectors";

export default { actions, namespace, reducer, selectors };